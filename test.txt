Nx =  20;
Ny =  20;
x_range = [0,1];
y_range = [0,1];
epsilon = 1e-6;
num_points = 4;
func = @(x,y)sin(pi*x).*sin(pi*y);
generate_points(x_range, y_range, func, Nx, Ny, num_points);
points = load('points.txt');
[mesh, A, S] = bogner_fox_schmit(Nx, Ny, x_range, y_range, points, epsilon);